import 'package:flutter/material.dart';

class CommentActivity extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
   return Container(
     child: Center(
       child: Text("COMMENT",style: TextStyle(fontSize: 20,fontWeight: FontWeight.bold),),
     ),
   );
  }
  
}